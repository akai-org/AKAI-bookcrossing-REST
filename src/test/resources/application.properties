# Application settings
server.port=8080
spring.application.name=AKAI-Bookcrossing

# Spring Data connection
spring.datasource.url=jdbc:h2:mem:bookcrossing;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
spring.datasource.username=sa
spring.datasource.password=q1w2e3r4
spring.datasource.driver-class-name=org.h2.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

#Liquibase configuration
spring.liquibase.change-log=classpath:db-changelog/db.changelog-master.xml

# Security configuration
spring.jwt.secret=AVeryLongLongLongAndReallyReallyReallySecureImportantJSONWebTokenSecret
spring.google.client-id=some-client-id.apps.googleusercontent.com

# Google Drive Configuration
spring.application.google-drive.source=some-drive-source
spring.application.google-drive.credentials=/credentials.json